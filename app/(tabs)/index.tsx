import { Screen } from '@/src/components/Screen';
import { Text } from 'react-native';

export default function Today() {
    return (
        <Screen>
            <Text>Dnes</Text>
        </Screen>
    );
}
